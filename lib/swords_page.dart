import 'package:flutter/material.dart';

class SwordsPage extends StatelessWidget {
  const SwordsPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        'Swords Page',
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}